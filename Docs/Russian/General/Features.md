# Функции и возможности

HyPrism предоставляет обширный набор функций для обычных игроков и опытных пользователей.

---

## Содержание

- [Идентификация и управление профилями](#-идентификация-и-управление-профилями)
- [Управление игровым клиентом](#-управление-игровым-клиентом)
- [Система модов](#-система-модов)
- [Пользовательский интерфейс](#-пользовательский-интерфейс)
- [Интеграции](#-интеграции)
- [Локализация](#-локализация)
- [Логирование и диагностика](#-логирование-и-диагностика)
- [Конфигурация](#-конфигурация)
- [Дополнительные ресурсы](#-дополнительные-ресурсы)

---

## 👤 Идентификация и управление профилями

### Система профилей

| Функция | Описание |
|---------|----------|
| **Множественные профили** | Создание и переключение между несколькими профилями |
| **Кастомизация** | Изменение никнейма и аватара |
| **Независимые настройки** | Каждый профиль хранит свои настройки |
| **Сохранение сессии** | Безопасное хранение токенов для автоматического входа |

### Сервисы

- `ProfileService` — хранение и загрузка профилей
- `ProfileManagementService` — высокоуровневые операции
- `UserIdentityService` — управление UUID и идентификацией
- `SkinService` — кастомные скины

---

## 🎮 Управление игровым клиентом

### Умное обновление

```mermaid
graph LR
    Check[Проверка версии] --> Download[Загрузка патча]
    Download --> Butler[Butler Apply]
    Butler --> Verify[Верификация]
    Verify --> Ready[Готово к запуску]
```

| Компонент | Функция |
|-----------|---------|
| **VersionService** | Получение списка доступных версий |
| **ButlerService** | Дифференциальные патчи через itch.io Butler |
| **DownloadService** | Загрузка с отображением прогресса |
| **ClientPatcher** | Бинарное патчирование исполняемых файлов |

### Бинарный патчинг

`ClientPatcher` — критический компонент для:
- Применения исправлений к игровому клиенту
- Включения дополнительных функций
- Верификации целостности файлов через хэши

### Множественные версии

```
Instances/
├── release/
│   ├── latest/          # Автообновляемая версия
│   └── v123/            # Закреплённая версия
└── pre-release/
    └── latest/
```

### Параметры запуска

- JVM аргументы (при необходимости)
- Кастомные флаги запуска
- Переменные окружения

---

## 🔧 Система модов

### Менеджер модов

| Функция | Описание |
|---------|----------|
| **Поиск модов** | Поиск по CurseForge и другим источникам |
| **Установка одним кликом** | Автоматическая загрузка и установка |
| **Разрешение зависимостей** | Автоматическая установка зависимых модов |
| **Удаление** | Полное удаление с очисткой |

### Сервис

```csharp
public class ModService
{
    Task<List<ModInfo>> SearchModsAsync(string query);
    Task InstallModAsync(ModInfo mod, string instancePath);
    Task UninstallModAsync(string modId, string instancePath);
    List<InstalledMod> GetInstalledMods(string instancePath);
}
```

---

## 🖥️ Пользовательский интерфейс

### Дизайн

- **Тёмная тема** — современный тёмный дизайн
- **Стеклянные эффекты** — полупрозрачные элементы
- **Адаптивный layout** — подстройка под размер окна
- **Плавные анимации** — переходы и эффекты

### Компоненты

| Категория | Компоненты |
|-----------|------------|
| **Buttons** | PrimaryButton, IconButton, CloseButton |
| **Cards** | NewsCard, NoticeCard |
| **Inputs** | TextBox, ComboBox, Slider |
| **Navigation** | Sidebar, Tabs |
| **Layouts** | Modal, Overlay, Grid |

### Система оверлеев

```csharp
// DashboardViewModel
[ObservableProperty] bool IsSettingsOpen;
[ObservableProperty] bool IsProfileEditorOpen;
[ObservableProperty] bool IsModManagerOpen;
```

### Темизация

- **Акцентный цвет** — настраивается пользователем
- **ThemeService** — динамическое применение тем
- **Цветовая палитра** — через XAML ресурсы

```csharp
ThemeService.Instance.ApplyAccentColor("#FF5500");
```

---

## 🌐 Интеграции

### Discord Rich Presence

| Состояние | Отображение |
|-----------|-------------|
| В лаунчере | "Browsing launcher" |
| Загрузка | "Downloading update..." |
| В игре | "Playing Hytale" |

```csharp
_discordService.UpdatePresence("Playing Hytale", $"Version {version}");
```

### Лента новостей

- Загрузка новостей с Hytale.com
- Рендеринг в карточках `NewsCard`
- Кэширование для офлайн режима

### Сервисы интеграций

| Сервис | Назначение |
|--------|------------|
| `DiscordService` | Discord RPC |
| `NewsService` | Загрузка новостей |
| `GitHubService` | Проверка обновлений лаунчера |
| `BrowserService` | Открытие внешних ссылок |

---

## 🌍 Локализация

### Поддерживаемые языки

| Код | Язык |
|-----|------|
| `en-US` | English |
| `ru-RU` | Русский |
| `de-DE` | Deutsch |
| `es-ES` | Español |
| `fr-FR` | Français |
| `ja-JP` | 日本語 |
| `ko-KR` | 한국어 |
| `pt-BR` | Português (Brasil) |
| `tr-TR` | Türkçe |
| `uk-UA` | Українська |
| `zh-CN` | 简体中文 |
| `be-BY` | Беларуская |

### Реактивное переключение

```csharp
// Смена языка без перезапуска
LocalizationService.Instance.CurrentLanguage = "ru-RU";

// Реактивная подписка
LocalizationService.Instance.GetObservable("button.play")
    .Subscribe(text => PlayButtonText = text);
```

### Формат файлов

`Assets/Locales/{code}.json`:
```json
{
  "_langName": "Русский",
  "_langCode": "ru-RU",
  "button": {
    "play": "Играть",
    "settings": "Настройки"
  }
}
```

### Fallback

При отсутствии перевода автоматически используется `en-US`.

---

## 📊 Логирование и диагностика

### Logger

```csharp
Logger.Info("Category", "Message");
Logger.Success("Download", "Completed!");
Logger.Error("Launch", $"Failed: {ex.Message}");
Logger.Progress("Download", 50, "Downloading...");
```

### Файлы логов

- Путь: `{AppDir}/Logs/{timestamp}.log`
- Формат: Serilog structured logging

---

## ⚙️ Конфигурация

### Config.cs

```csharp
public class Config
{
    public string UUID { get; set; }
    public string Nick { get; set; }
    public string VersionType { get; set; }  // "release" или "pre-release"
    public string? AccentColor { get; set; }
    public string Language { get; set; }
    public bool DiscordRPC { get; set; }
    // ...
}
```

### Путь к конфигурации

| ОС | Путь |
|----|------|
| Windows | `%APPDATA%/HyPrism/config.json` |
| Linux | `~/.config/HyPrism/config.json` |
| macOS | `~/Library/Application Support/HyPrism/config.json` |

---

## 📚 Дополнительные ресурсы

- [Architecture.md](../Technical/Architecture.md) — Архитектура системы
- [ServicesReference.md](../Technical/ServicesReference.md) — Справочник сервисов
- [UIComponentGuide.md](../Development/UIComponentGuide.md) — Создание компонентов
